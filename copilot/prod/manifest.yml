# The manifest for the "no" service.
# Read the full specification for the "Load Balanced Web Service" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/

# Your service name will be used in naming your resources like log groups, ECS services, etc.
name: prod
type: Load Balanced Web Service

# Distribute traffic to your service.
http:
  # Requests to this path will be forwarded to your service.
  # To match all requests you can use the "/" path.
  path: '/'
  # You can specify a custom health check path. The default is "/".
  # healthcheck: '/'
  
storage:
  volumes:
    data:
      path: '/data'
      read_only: false
      efs:
        id: fs-0be892f4a707bb7b6
        
        
image:
  build:
    dockerfile: server/Dockerfile
  port: 8080
  command: ["java","-jar","/app.jar", "--spring.profiles.active=h2"]

cpu: 256       # Number of CPU units for the task.
memory: 1024    # Amount of memory in MiB used by the task.
count: 1       # Number of tasks that should be running in your service.
exec: true     # Enable running commands in your container.
network:
  connect: true # Enable Service Connect for intra-environment traffic between services.

command: ["java","-jar","/app.jar", "--spring.profiles.active=h2"]

variables:
  H2_FILENAME: /data/h2_db
  BOOTSTRAP_OWNER_ALLOWED: true

secrets:
  JWT_SIGNING_KEY:
    secretsmanager: 'arn:aws:ssm:eu-central-1:826764853040:parameter/copilot/wwwp/prod/secrets/JWT_SIGNING_KEY'

# You can override any of the values defined above by environment.
#environments:
#  test:
#    count: 2               # Number of tasks to run for the "test" environment.
#    deployment:            # The deployment strategy for the "test" environment.
#       rolling: 'recreate' # Stops existing tasks before new ones are started for faster deployments.