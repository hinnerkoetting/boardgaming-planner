meta {
  name: Bootstrap admin
  type: http
  seq: 3
}

post {
  url: http://localhost:8080/api/auth/bootstrapAdmin/{{player_id}}
  body: none
  auth: none
}

body:json {
  {
    "login": "user",
    "password": "user"
  }
}

assert {
  res.status: eq 200
}
