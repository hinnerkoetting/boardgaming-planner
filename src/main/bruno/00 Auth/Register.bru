meta {
  name: Register
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/auth/register
  body: json
  auth: none
}

body:json {
  {
    "login": "user3",
    "password": "user"
  }
}

assert {
  res.status: eq 200
}

script:post-response {
  if (res.body.token) {
    bru.setVar("access_token", res.body.token)
  }
}
