meta {
  name: Delete game
  type: http
  seq: 3
}

delete {
  url: http://localhost:8080/api/games/{{gameId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "id": "test"
  }
}

assert {
  res.status: eq 200
}
