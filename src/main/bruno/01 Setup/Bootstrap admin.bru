meta {
  name: Bootstrap admin
  type: http
  seq: 2
}

post {
  url: {{server}}/api/players/{{playerId}}/roles/bootstrapOwner
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "login": "user",
    "password": "user"
  }
}

assert {
  res.status: eq 200
}
