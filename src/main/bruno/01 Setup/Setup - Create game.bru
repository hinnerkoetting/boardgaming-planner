meta {
  name: Setup - Create game
  type: http
  seq: 3
}

post {
  url: http://localhost:8080/api/games
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "name": "Gloomhaven"
  }
}

assert {
  res.status: eq 201
}

script:post-response {
  bru.setVar("gameId", res.body.id)
  
}
