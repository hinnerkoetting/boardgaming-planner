meta {
  name: Setup - Create player
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/api/players
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "name": "Hinnerk3"
  }
}

assert {
  res.status: in 201, 409
}

script:post-response {
  bru.setVar("playerId", res.body.id)
}
