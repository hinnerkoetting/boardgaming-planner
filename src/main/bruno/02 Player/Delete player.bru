meta {
  name: Delete player
  type: http
  seq: 2
}

delete {
  url: {{server}}/api/players/{{playerId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

assert {
  res.status: eq 200
}
