meta {
  name: List players
  type: http
  seq: 1
}

headers {
  Authorization: Bearer {{access_token}}
}

get {
  url: http://localhost:8080/api/players
  body: none
  auth: none
}

assert {
  res.status: eq 200
}
