meta {
  name: List players
  type: http
  seq: 1
}

get {
  url: {{server}}/api/players
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

assert {
  res.status: eq 200
}
