meta {
  name: Delete gamegroup
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/api/gameGroups/{{gameGroupId}}
  body: none
  auth: none
}

body:json {
  {
    "name": "test"
  }
}

assert {
  res.status: eq 201
}

script:post-response {
  bru.setVar("gameGroupId", res.body.id)
}
