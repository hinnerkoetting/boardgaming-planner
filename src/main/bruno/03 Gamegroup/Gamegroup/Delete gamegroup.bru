meta {
  name: Delete gamegroup
  type: http
  seq: 4
}

delete {
  url: {{server}}/api/gameGroups/{{gameGroupId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "name": "test"
  }
}

assert {
  res.status: in 403, 200
}
