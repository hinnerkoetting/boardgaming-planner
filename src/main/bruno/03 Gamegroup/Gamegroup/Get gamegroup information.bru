meta {
  name: Get gamegroup information
  type: http
  seq: 3
}

get {
  url: {{server}}/api/gameGroups/{{gameGroupId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

vars:pre-request {
  gameGroupId: 
}

assert {
  res.status: eq 200
}
