meta {
  name: List gamegroups
  type: http
  seq: 2
}

get {
  url: {{server}}/api/gameGroups
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

vars:post-response {
  gameGroupId: res.body[0].id
}

assert {
  res.status: eq 200
}
