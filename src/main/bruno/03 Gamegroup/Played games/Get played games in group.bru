meta {
  name: Get played games in group
  type: http
  seq: 2
}

get {
  url: {{server}}/api/gameGroups/{{gameGroupId}}/games
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

vars:pre-request {
  gameGroupId: 1
}

assert {
  res.status: eq 200
}
