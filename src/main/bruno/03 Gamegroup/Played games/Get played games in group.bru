meta {
  name: Get played games in group
  type: http
  seq: 2
}

get {
  url: http://localhost:8080/api/gameGroups/{{gameGroupId}}/playedGames
  body: none
  auth: none
}

assert {
  res.status: eq 200
}
