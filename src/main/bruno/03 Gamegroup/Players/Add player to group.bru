meta {
  name: Add player to group
  type: http
  seq: 1
}

post {
  url: {{server}}/api/gameGroups/{{gameGroupId}}/players
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  { 
    "id": {{playerId}}
  }
}

body:text {
  1
}

assert {
  res.status: in 201, 409
}
