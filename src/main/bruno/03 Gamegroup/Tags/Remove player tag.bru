meta {
  name: Remove player tag
  type: http
  seq: 4
}

delete {
  url: {{server}}/api/gameGroups/{{gameGroupId}}/{{gameId}}/gameGroupTag/{{tagId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

vars:pre-request {
  gameGroupId: 1
  gameId: 1
  tagId: 3
}

assert {
  res.status: eq 200
}
