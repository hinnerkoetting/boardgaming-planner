meta {
  name: Delete game
  type: http
  seq: 2
}

delete {
  url: {{server}}/api/games/{{gameId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "id": "test"
  }
}

assert {
  res.status: eq 200
}
