meta {
  name: Remove tag from game
  type: http
  seq: 2
}

delete {
  url: http://localhost:8080/api/games/{{gameId}}/globalTag/{{tagId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

vars:pre-request {
  role: ROLE_ADMIN
}

assert {
  res.status: eq 200
}
