meta {
  name: Setup - Create gamegroup
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/api/gameGroups
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "name": "Boardgames"
  }
}

assert {
  res.status: eq 201
}

script:post-response {
  bru.setVar("gameGroupId", res.body.id)
}
