meta {
  name: Create rating
  type: http
  seq: 2
}

put {
  url: {{server}}/api/ratings
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "playerId": {{playerId}},
    "gameId": {{gameId}},
    "gameGroupId": {{gameGroupId}},
    "rating": 1
  }
}
