meta {
  name: Delete rating
  type: http
  seq: 2
}

delete {
  url: http://localhost:8080/api/ratings
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "playerId": {{playerId}},
    "gameId": {{gameId}},
    "gameGroupId": {{gameGroupId}}
  }
}
