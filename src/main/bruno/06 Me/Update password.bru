meta {
  name: Update password
  type: http
  seq: 4
}

put {
  url: http://localhost:8080/api/me/password
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "oldPassword": "user",
    "newPassword": "user"
  }
}

assert {
  res.status: eq 200
}
