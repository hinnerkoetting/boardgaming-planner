meta {
  name: Add role
  type: http
  seq: 2
}

put {
  url: {{server}}/api/players/6352/roles/{{role}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "login": "user",
    "password": "user"
  }
}

vars:pre-request {
  role: ROLE_ADMIN
}

assert {
  res.status: eq 200
}
