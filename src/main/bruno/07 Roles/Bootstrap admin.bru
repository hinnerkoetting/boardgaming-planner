meta {
  name: Bootstrap admin
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/roles/{{player_id}}/bootstrapOwner
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "login": "user",
    "password": "user"
  }
}

assert {
  res.status: eq 200
}
