meta {
  name: Add Tag
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/tags
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "id": "TAG_ID",
    "description": "DESCRIPTION",
    "order": 1,
    "type": "GLOBAL"
  }
}

vars:pre-request {
  role: ROLE_ADMIN
}

assert {
  res.status: eq 200
}
