meta {
  name: Delete Tag
  type: http
  seq: 4
}

delete {
  url: http://localhost:8080/api/tags/{{tag_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

vars:pre-request {
  role: ROLE_ADMIN
}

assert {
  res.status: eq 200
}
