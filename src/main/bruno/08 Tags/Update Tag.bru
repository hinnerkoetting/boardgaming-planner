meta {
  name: Update Tag
  type: http
  seq: 3
}

put {
  url: http://localhost:8080/api/tags/{{tag_id}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "description": "DESCRIPTION",
    "ranking": 1,
    "type": "GLOBAL"
  }
}

vars:pre-request {
  role: ROLE_ADMIN
}

assert {
  res.status: eq 200
}
