meta {
  name: Update Tag
  type: http
  seq: 3
}

put {
  url: {{server}}/api/tags/{{tagId}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "description": "DESCRIPTION",
    "ranking": null,
    "type": "GLOBAL"
  }
}

vars:pre-request {
  role: ROLE_ADMIN
}

assert {
  res.status: eq 200
}
