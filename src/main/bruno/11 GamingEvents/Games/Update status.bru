meta {
  name: Update status
  type: http
  seq: 3
}

put {
  url: {{server}}/api/gamingEvents/{{gamingEventId}}/game/{{gameId}}/status
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "status": "PLAYED"
  }
  
}

vars:pre-request {
  gameGroupId: 1
  gameId: 1
}

assert {
  res.status: eq 200
}
