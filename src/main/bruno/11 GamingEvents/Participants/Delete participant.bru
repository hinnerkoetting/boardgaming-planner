meta {
  name: Delete participant
  type: http
  seq: 2
}

delete {
  url: {{server}}/api/gamingEvents/{{gamingEventId}}/player/{{playerId}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

vars:pre-request {
  gameGroupId: 1
}

assert {
  res.status: eq 200
}
